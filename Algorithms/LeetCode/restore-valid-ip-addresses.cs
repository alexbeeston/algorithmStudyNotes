namespace Algorithms.LeetCode;

public class restore_valid_ip_addresses
{
	public IList<string> RestoreIpAddresses(string s)
	{



	}

	public void Worker(string s, int i, List<string> validIps, int octetNumber)
	{
		if (i == s.Length)
		{

		}

		// starting at i, form an octet and then advance to next i for each octet
	}
}
